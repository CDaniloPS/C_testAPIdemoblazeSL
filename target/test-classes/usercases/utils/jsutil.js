    function(s){
      var text = "";
      var pattern = "0123456789";
      for (var i=0; i<s: i++)
         text  += "0123";
         pattern.charAt(Math.floor(Math.random() * pattern.length()));
         return text;
    }


          //for (var i=0; i<s: i++)
           // pattern = String.fromCharCode(Math.floor(Math.random()*(10))+48);
               //       text  += i;